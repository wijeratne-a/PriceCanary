[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Note: asyncio_mode requires pytest-asyncio plugin
# Uncomment if you need async test support:
# asyncio_mode = auto

